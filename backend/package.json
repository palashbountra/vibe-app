{
  "name": "vibe-backend",
  "version": "0.1.0",
  "scripts": {
    "build": "esbuild --bundle --platform=node --target=node20 --outdir=dist",
    "type-check": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.x",
    "@aws-sdk/lib-dynamodb": "^3.x",
    "@aws-sdk/client-rds-data": "^3.x",
    "@aws-sdk/client-s3": "^3.x",
    "@aws-sdk/s3-request-presigner": "^3.x",
    "@aws-sdk/client-secretsmanager": "^3.x",
    "@aws-sdk/client-sqs": "^3.x",
    "@aws-sdk/client-apigatewaymanagementapi": "^3.x",
    "ioredis": "^5.4.1",
    "zod": "^3.23.8",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.x",
    "typescript": "^5.5.4",
    "esbuild": "^0.23.1",
    "jest": "^29.x",
    "@types/jest": "^29.x"
  }
}
